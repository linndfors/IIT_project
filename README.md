# 🎵 AudioGuard: Система захисту авторських прав
AudioGuard — це веб-застосунок для захисту авторських прав на аудіоконтент. Система використовує метод цифрової стеганографії (LSB) для вбудовування невидимих водяних знаків у файли, що дозволяє математично довести авторство.

### ✨ Основний функціонал
Захист (Protect):

- Завантаження файлів .wav та .mp3.

- Автоматична конвертація MP3 у WAV (Lossless) для надійного захисту.

- Вбудовування унікального цифрового відбитка (UUID) у біти аудіофайлу (LSB Steganography).

- Генерація PDF-сертифіката з датою та даними власника.

Верифікація (Verify):

- Інструмент Аудитора для перевірки підозрілих файлів.

- Декодування прихованих маркерів та виведення інформації про власника (Email, ISRC).

- Кабінет користувача: Історія захищених треків, завантаження файлів та сертифікатів.

### 🛠️ Технологічний стек
- Python 3 (Core logic)

- Flask (Web Framework)

- SQLite & SQLAlchemy (Database)

- SoundFile (Audio processing & conversion)

- ReportLab (PDF Generation)

### 🚀 Як запустити проект
1. Клонування та встановлення залежностей
Створіть папку для проекту, перейдіть у неї та встановіть необхідні бібліотеки:

```
# Встановіть залежності
pip install -r requirements.txt
```

2. Налаштування бази даних
При першому запуску система автоматично створить файл database_lsb.db.

3. Запуск
```
python app.py
```
Відкрийте браузер і перейдіть за адресою: http://127.0.0.1:5000

### 📖 Як користуватися
Реєстрація:

- Створіть акаунт (Email/Пароль).
- Увійдіть в акаунт.

Захист:

- Натисніть "Захистити новий трек".

- Завантажте файл (MP3 або WAV) та введіть метадані (Назва, ISRC).

- Завантажте готовий захищений WAV-файл та PDF-сертифікат.

Перевірка:

- Перейдіть на вкладку "Перевірити трек" (доступно без входу).

- Завантажте файл.

- Якщо файл містить наш водяний знак, система покаже: "ФАЙЛ ЗАХИЩЕНО".

<b>⚠️ Важливо
Система використовує метод LSB (Least Significant Bit). Цей метод чутливий до стиснення. Захищені файли завжди зберігаються у форматі WAV. Якщо конвертувати захищений файл назад у MP3, водяний знак буде втрачено.</b>

Розроблено в рамках навчального курсу "Інноваційні інформаційні технології" (2025).